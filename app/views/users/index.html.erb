<% provide(:title, @title) %>
<h1><%= @title %></h1>

<div class="row">
  <div class="search_form">
    <%= render 'users/search_form' %>
  </div>
</div>

<%= will_paginate %>

<% unless @users.empty? %>
  <ul class="users">
    <%= render @users %>
  </ul>
<% else %>
  <p>ユーザーが見つかりませんでした。<br>
    お探しのユーザー名をご確認ください。</p>
<% end %>

<%= will_paginate %>