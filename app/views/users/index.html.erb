<% provide(:title, @title) %>
<h1><%= @title %></h1>

<div class="row">
  <div class="search_form">
    <%= render 'users/search_form' %>
  </div>
</div>

<%= will_paginate %>

<% unless @users.empty? %>
  <ul class="users">
    <%= render @users %>
  </ul>
<% else %>
  <p>ユーザーが見つかりませんでした。<br>
    お探しのユーザー名をご確認ください。</p>
<% end %>

<%= will_paginate %>

  <%= javascript_include_tag 'bubbly.js' %>
<script>bubbly({
  blur:200,
  colorStart: '#BAD3FF',
  colorStop: '#D9E5FF',
  radiusFunc:() => 5 + Math.random() * 75,
  angleFunc:() => -Math.PI / 2,
  velocityFunc:() => Math.random() * 2,
  bubbleFunc:() => `hsla(${200 + Math.random() * 50}, 100%, 65%, .1)`,
  bubbles:150
});</script>